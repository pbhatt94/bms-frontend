export enum Gender {
  male = 'male',
  female = 'female',
  other = 'other',
}

export enum Role {
  user = 'user',
  admin = 'admin',
  customer = 'customer',
}

export type ResponseType = 'SUCCESS' | 'ERROR';
